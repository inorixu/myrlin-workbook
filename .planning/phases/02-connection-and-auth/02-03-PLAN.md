---
phase: 02-connection-and-auth
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - mobile/stores/auth-store.ts
  - mobile/components/BiometricGate.tsx
  - mobile/components/ConnectionDot.tsx
  - mobile/components/ServerMenu.tsx
  - mobile/hooks/useConnectionHealth.ts
  - mobile/app/_layout.tsx
  - mobile/app/(tabs)/_layout.tsx
autonomous: false
requirements:
  - CONN-04
  - CONN-05
  - CONN-06
  - CONN-07
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "User can enable biometric lock and the app requires Face ID or fingerprint to unlock after backgrounding"
    - "Connection status indicator is visible on every tab screen showing connected or disconnected state"
    - "App auto-reconnects with exponential backoff when connection drops"
    - "User can switch between multiple connected servers"
    - "Switching servers clears stale state and re-checks connectivity"
    - "User can log out (disconnect) from the active server from any tab screen"
  artifacts:
    - path: "mobile/stores/auth-store.ts"
      provides: "Biometric lock state management"
      exports: ["useAuthStore"]
    - path: "mobile/components/BiometricGate.tsx"
      provides: "Full-screen lock overlay with biometric prompt"
      min_lines: 40
    - path: "mobile/components/ConnectionDot.tsx"
      provides: "Colored status dot component"
      min_lines: 20
    - path: "mobile/components/ServerMenu.tsx"
      provides: "Server info popover with disconnect and switch server actions"
      min_lines: 40
    - path: "mobile/hooks/useConnectionHealth.ts"
      provides: "Health check polling with exponential backoff"
      exports: ["useConnectionHealth"]
  key_links:
    - from: "mobile/hooks/useConnectionHealth.ts"
      to: "mobile/stores/server-store.ts"
      via: "reads activeServer, writes connectionStatus"
      pattern: "setConnectionStatus|activeServer"
    - from: "mobile/hooks/useConnectionHealth.ts"
      to: "mobile/services/api-client.ts"
      via: "calls checkAuth on active server"
      pattern: "checkAuth|createAPIClient"
    - from: "mobile/components/BiometricGate.tsx"
      to: "mobile/stores/auth-store.ts"
      via: "reads isLocked, calls unlock"
      pattern: "isLocked|unlock"
    - from: "mobile/app/_layout.tsx"
      to: "mobile/components/BiometricGate.tsx"
      via: "renders BiometricGate overlay when locked"
      pattern: "BiometricGate|isLocked"
    - from: "mobile/components/ServerMenu.tsx"
      to: "mobile/stores/server-store.ts"
      via: "calls logout and removeServer"
      pattern: "logout|removeServer"
    - from: "mobile/components/ServerMenu.tsx"
      to: "mobile/services/api-client.ts"
      via: "calls API logout before clearing local state"
      pattern: "apiClient\\.logout"
---

<objective>
Build multi-server switching, biometric app lock, connection health monitoring with auto-reconnect, the always-visible connection status indicator, and a server menu with logout capability.

Purpose: Makes the app robust (auto-reconnect), secure (biometric lock), multi-server capable, and allows users to disconnect from any screen (AUTH-03). These are the advanced connection features that make the app production-ready.
Output: Auth store, BiometricGate component, ConnectionDot component, ServerMenu component, health check hook, updated layouts.
</objective>

<execution_context>
@C:/Users/Arthur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Arthur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-connection-and-auth/02-RESEARCH.md
@.planning/phases/02-connection-and-auth/02-01-SUMMARY.md

@mobile/stores/theme-store.ts
@mobile/hooks/useTheme.ts

<interfaces>
<!-- From Plan 01 outputs -->
From mobile/stores/server-store.ts:
```typescript
export const useServerStore: {
  servers: ServerConnection[];
  activeServerId: string | null;
  connectionStatus: ConnectionStatus;
  activeServer: ServerConnection | null;
  addServer: (...) => Promise<void>;
  removeServer: (id: string) => void;
  switchServer: (id: string) => void;
  setConnectionStatus: (status: ConnectionStatus) => void;
  updateToken: (serverId: string, token: string) => void;
  logout: (serverId: string) => void;
};
```

From mobile/services/api-client.ts:
```typescript
class MyrlinAPIClient {
  checkAuth(): Promise<{ authenticated: boolean }>;
  login(password: string): Promise<LoginResponse>;
  logout(): Promise<{ success: boolean }>;
}
export function createAPIClient(baseUrl: string, token: string): MyrlinAPIClient;
```

From mobile/theme/types.ts:
```typescript
interface MyrlinTheme {
  colors: { base, text, blue, green, red, yellow, surface0, bgPrimary, textPrimary, accent, error, success, warning, ... };
  spacing: { xs, sm, md, lg, xl };
  radius: { sm, md, lg, full };
  animation: { fast, normal };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth store, biometric gate, and connection health hook</name>
  <files>mobile/stores/auth-store.ts, mobile/components/BiometricGate.tsx, mobile/hooks/useConnectionHealth.ts, mobile/components/ConnectionDot.tsx</files>
  <action>
1. **Create mobile/stores/auth-store.ts**:
   - Zustand store with MMKV persistence (same adapter pattern as theme-store.ts)
   - State: `biometricEnabled: boolean` (user preference), `isLocked: boolean` (current lock state)
   - Actions:
     - `setBiometricEnabled(enabled: boolean)`: toggles biometric lock preference. When enabling, call `LocalAuthentication.hasHardwareAsync()` and `isEnrolledAsync()` first. If no hardware or not enrolled, do not enable (return false or throw).
     - `lock()`: sets isLocked=true (call on app background)
     - `unlock()`: sets isLocked=false (call after successful biometric auth)
   - Persist only `biometricEnabled` to MMKV (not `isLocked`, which starts as false on launch)
   - Use the existing MMKV pattern from theme-store.ts with a dedicated `createMMKV({ id: 'myrlin-auth' })` instance

2. **Create mobile/components/BiometricGate.tsx**:
   - Full-screen overlay (StyleSheet.absoluteFillObject, zIndex 9999)
   - Background: theme.colors.base
   - Centered content: lock icon (Ionicons 'lock-closed-outline'), "Myrlin is locked" text, "Tap to unlock" pressable button
   - On mount (useEffect), automatically call `LocalAuthentication.authenticateAsync({ promptMessage: 'Unlock Myrlin', fallbackLabel: 'Use Passcode', disableDeviceFallback: false })`
   - On success: call `useAuthStore.getState().unlock()`
   - On failure: show the "Tap to unlock" button for manual retry
   - Use Reanimated for fade-in animation (opacity from 0 to 1, 200ms)
   - All colors from useTheme()

3. **Create mobile/hooks/useConnectionHealth.ts**:
   - Takes no arguments, reads activeServer from useServerStore
   - useEffect that runs when `activeServer?.id` changes:
     - Creates a polling loop with exponential backoff
     - Initial interval: 5000ms
     - On success (authenticated=true): set connectionStatus 'connected', reset backoff to 5000ms
     - On success (authenticated=false): set connectionStatus 'token-expired'
     - On network error: set connectionStatus 'disconnected', double backoff (max 60000ms)
     - Cleanup: clearTimeout on unmount or server change
   - Also listens to AppState changes (from 'react-native'): when app comes to foreground, immediately check health (do not wait for next interval)
   - When app goes to background and biometricEnabled is true, call `useAuthStore.getState().lock()`
   - Export `useConnectionHealth` hook

4. **Create mobile/components/ConnectionDot.tsx**:
   - Small colored dot component (8x8, borderRadius full)
   - Props: none (reads connectionStatus from useServerStore directly)
   - Colors from theme:
     - 'connected' = theme.colors.green
     - 'connecting' = theme.colors.yellow
     - 'disconnected' = theme.colors.red
     - 'token-expired' = theme.colors.peach
   - Animated pulse for 'connecting' state (Reanimated opacity animation, repeating)
   - Optional `showLabel` prop that shows text label next to dot
   - Optional `onPress` prop for parent to handle taps
   - Add JSDoc comments
  </action>
  <verify>
    <automated>cd "C:/Users/Arthur/Desktop/claude-workspace-manager/mobile" && npx tsc --noEmit</automated>
  </verify>
  <done>Auth store manages biometric preference in MMKV. BiometricGate renders lock overlay with auto-prompt. ConnectionHealth hook polls with exponential backoff and handles foreground/background transitions. ConnectionDot shows colored status indicator with optional onPress.</done>
</task>

<task type="auto">
  <name>Task 2: Server menu, logout UI, and layout wiring</name>
  <files>mobile/components/ServerMenu.tsx, mobile/app/_layout.tsx, mobile/app/(tabs)/_layout.tsx</files>
  <action>
1. **Create mobile/components/ServerMenu.tsx**:
   - A modal/popover component that shows server info and actions
   - Triggered by tapping the ConnectionDot in the header
   - Content:
     - Active server name and URL (from useServerStore.activeServer)
     - Connection status text and colored dot
     - If multiple servers: list of other servers with "Switch" action for each
     - "Add Server" action (navigates to `/(auth)/onboarding`)
     - Separator line
     - **"Disconnect" button** (red/destructive styling using theme.colors.red):
       - Calls `createAPIClient(server.url, server.token).logout()` to invalidate the token server-side
       - Then calls `useServerStore.getState().removeServer(serverId)` to clear local state
       - If no servers remain after removal, navigates to `/(auth)/onboarding`
       - If other servers remain, switches to the next available server
   - Use React Native Modal or a custom overlay (Pressable backdrop to dismiss)
   - Style: dark surface (theme.colors.surface0), rounded corners (theme.radius.lg), shadow
   - Position: anchored below the header right area
   - All text and colors from useTheme()
   - Add JSDoc comments on the component and disconnect handler

2. **Modify mobile/app/_layout.tsx** to add biometric gate:
   - Import `useAuthStore` from '@/stores/auth-store'
   - Import `BiometricGate` from '@/components/BiometricGate'
   - Read `isLocked` and `biometricEnabled` from auth store
   - After the hydration check and auth gate (from Plan 02), render BiometricGate overlay:
     ```tsx
     {biometricEnabled && isLocked && <BiometricGate />}
     ```
   - Place it AFTER the main layout content so it overlays on top
   - Do NOT remove any existing code

3. **Modify mobile/app/(tabs)/_layout.tsx** to add connection dot with server menu and health check:
   - Import `useConnectionHealth` from '@/hooks/useConnectionHealth'
   - Import `ConnectionDot` from '@/components/ConnectionDot'
   - Import `ServerMenu` from '@/components/ServerMenu'
   - Call `useConnectionHealth()` at the top level of the tabs layout (starts health polling)
   - Add a `serverMenuVisible` state (useState boolean)
   - Add `ConnectionDot` to `screenOptions.headerRight` with `onPress` toggling `serverMenuVisible`:
     ```tsx
     headerRight: () => (
       <Pressable onPress={() => setServerMenuVisible(true)}>
         <ConnectionDot showLabel />
       </Pressable>
     )
     ```
   - Render `<ServerMenu visible={serverMenuVisible} onClose={() => setServerMenuVisible(false)} />` inside the tabs layout
   - This makes the connection dot tappable on ALL tab screens, opening the server menu with disconnect option
   - Preserve all existing tab configuration
  </action>
  <verify>
    <automated>cd "C:/Users/Arthur/Desktop/claude-workspace-manager/mobile" && npx tsc --noEmit</automated>
  </verify>
  <done>ServerMenu provides disconnect (logout) from any tab screen (AUTH-03). Root layout shows biometric gate when locked. Tab layout starts health polling, shows tappable connection dot in header that opens the server menu. All compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify connection features end-to-end</name>
  <action>Human verification of multi-server support, biometric app lock, auto-reconnect, connection status indicator, and logout.</action>
  <what-built>Multi-server support, biometric app lock, auto-reconnect with connection status indicator, and server menu with logout</what-built>
  <how-to-verify>
    1. Start the Myrlin server: `npm run gui`
    2. Run mobile app on device with biometrics (physical device preferred)
    3. Verify connection dot shows green "Connected" in tab headers
    4. Tap the connection dot, verify the server menu appears showing server name, URL, and "Disconnect" button
    5. Stop the server, verify dot turns red "Disconnected" within ~10 seconds
    6. Restart the server, verify dot turns green again (auto-reconnect)
    7. Tap connection dot, tap "Disconnect", verify the app logs out and returns to the onboarding screen
    8. Reconnect via manual connect
    9. (If physical device) Open Settings in the app, enable biometric lock
    10. Background the app, re-open it, verify Face ID/fingerprint prompt appears
    11. After unlocking, verify app is usable
    12. (Optional) Connect to a second server, verify you can switch between them via the server menu
  </how-to-verify>
  <verify>Manual verification by user</verify>
  <done>User confirms connection features work: status dot, server menu with logout, auto-reconnect, biometric lock</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd mobile && npx tsc --noEmit`
2. Connection dot visible on all tab screens, tappable to open server menu
3. Server menu shows disconnect option that logs out and clears state
4. Health check recovers after server restart
5. Biometric gate blocks access when locked
6. Existing tests still pass: `npm test`
</verification>

<success_criteria>
- Connection status dot visible on every tab screen, changes color with server state
- Tapping connection dot opens server menu with server info, switch, and disconnect actions
- Disconnect calls server logout API, removes local server, returns to onboarding if no servers left
- Auto-reconnect works with exponential backoff (5s, 10s, 20s, 40s, 60s max)
- Biometric lock gates the entire app when enabled
- Server switching clears connection state and re-checks health
- All code compiles, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-connection-and-auth/02-03-SUMMARY.md`
</output>
