---
phase: 12-api-enhancement
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
  - 12-02
files_modified:
  - test/pagination.test.js
autonomous: true
requirements:
  - MTST-05
must_haves:
  truths:
    - "Integration tests verify session pagination with limit and offset"
    - "Integration tests verify status filter returns only matching sessions"
    - "Integration tests verify sort and order produce correct sequence"
    - "Integration tests verify search filters by name/topic substring"
    - "Integration tests verify workspaceId filter narrows to workspace sessions"
    - "Integration tests verify backward compatibility (no params returns old shape)"
    - "All tests pass in CI via npm test"
  artifacts:
    - path: "test/pagination.test.js"
      provides: "Integration tests for session pagination, scrollback, logs, and token refresh"
      min_lines: 100
  key_links:
    - from: "test/pagination.test.js"
      to: "src/state/store.js"
      via: "Direct store method testing for getPaginatedSessions"
      pattern: "getPaginatedSessions"
---

<objective>
Write integration tests for session pagination, scrollback pagination, logs pagination, and token refresh to verify all Phase 12 features work correctly with various filter combinations.

Purpose: MTST-05 requires integration tests that verify pagination with various filter combinations. These tests exercise the store methods directly (fast, no HTTP server needed) and ensure edge cases are covered.
Output: test/pagination.test.js with comprehensive test coverage.
</objective>

<execution_context>
@C:/Users/Arthur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Arthur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@test/token-persistence.test.js (existing test pattern for store setup/teardown)
@test/device-manager.test.js (existing test pattern)

<interfaces>
<!-- From 12-01 plan: new store method signature -->
```javascript
// store.getPaginatedSessions(options) returns:
{
  sessions: Session[],
  total: number,
  limit: number,
  offset: number,
  hasMore: boolean
}
// Options: { limit, offset, status, sort, order, search, workspaceId }
```

<!-- From 12-02 plan: new store method and scrollback -->
```javascript
// store.refreshDeviceToken(deviceId, newToken, newExpiresAt) returns updated device or null
// ptyManager.getScrollbackLines(sessionId, { lines, from }) returns:
{ lines: string[], total: number, from: number, hasMore: boolean }
```

<!-- Existing test patterns from token-persistence.test.js -->
```javascript
const path = require('path');
const fs = require('fs');
// Tests clear require.cache and delete state files between tests
// Use store.createSession, store.createWorkspace to seed test data
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration tests for session pagination</name>
  <files>test/pagination.test.js</files>
  <behavior>
    - Test: Default pagination returns sessions with limit 50 when pagination params present
    - Test: limit=2 offset=0 returns 2 sessions with hasMore=true when more exist
    - Test: limit=2 offset=2 returns next page with correct hasMore
    - Test: status=running filters to only running sessions
    - Test: status=stopped filters to only stopped sessions
    - Test: sort=name order=asc returns alphabetically sorted sessions
    - Test: sort=lastActive order=desc returns most recent first (default)
    - Test: sort=created order=asc returns oldest first
    - Test: search=keyword filters sessions matching name or topic
    - Test: search is case-insensitive
    - Test: workspaceId filter returns only sessions in that workspace
    - Test: Combined filters (status + search + workspaceId) work together
    - Test: limit clamped to max 100
    - Test: offset beyond total returns empty sessions array with correct total
    - Test: Backward compat: no pagination params returns all sessions in old { sessions } shape (tested via store.getAllSessionsList)
    - Test: Logs pagination: limit/offset slices session.logs correctly
    - Test: Token refresh: refreshDeviceToken updates token and expiresAt in pairedDevices
    - Test: Token refresh: old token no longer found by findDeviceByToken
  </behavior>
  <action>
Create test/pagination.test.js following the existing test patterns:

1. Use Node's built-in test runner (same as token-persistence.test.js and device-manager.test.js)
2. Setup: Clear require.cache for store, create fresh store instance, seed with test data:
   - Create 2 workspaces
   - Create 10+ sessions with varied names, statuses, topics, workspaceIds
   - Some running, some stopped, some with specific topics for search
   - Add a paired device for token refresh tests

3. Test getPaginatedSessions directly (no HTTP, fast):
   - Default behavior: limit=50, offset=0
   - Pagination: limit=3, offset=0, then limit=3, offset=3
   - Status filter: 'running' returns only running, 'stopped' returns only stopped
   - Sort: name/asc, lastActive/desc, created/asc
   - Search: substring match on name, topic, case-insensitive
   - WorkspaceId filter
   - Combined filters
   - Edge: offset > total returns empty array with correct total
   - Edge: limit clamped at 100

4. Test logs pagination:
   - Add 20 log entries to a session
   - Verify limit=5 offset=0 returns first 5
   - Verify limit=5 offset=15 returns last 5 with hasMore=false

5. Test token refresh:
   - Add a paired device with a known token
   - Call refreshDeviceToken with new token
   - Verify findDeviceByToken(oldToken) returns null
   - Verify findDeviceByToken(newToken) returns the device
   - Verify expiresAt updated

6. Teardown: Clean up state files.

Use descriptive test names. Follow existing assertion patterns (assert.strictEqual, assert.ok).
  </action>
  <verify>
    <automated>cd C:/Users/Arthur/Desktop/claude-workspace-manager && node test/pagination.test.js</automated>
  </verify>
  <done>
  - test/pagination.test.js exists with 15+ test cases
  - All tests pass via node test/pagination.test.js
  - npm test also passes (includes new test file)
  - Tests cover: limit/offset, status filter, sort/order, search, workspaceId, combined filters, edge cases, logs pagination, token refresh
  </done>
</task>

</tasks>

<verification>
- `node test/pagination.test.js` passes all tests
- `npm test` passes with no regressions
- Test coverage includes all filter combinations and edge cases
</verification>

<success_criteria>
MTST-05 satisfied: Integration tests for session pagination with various filters exist and pass.
Additionally covers logs pagination and token refresh store operations for completeness.
</success_criteria>

<output>
After completion, create `.planning/phases/12-api-enhancement/12-03-SUMMARY.md`
</output>
