---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - mobile/maestro/flows/app-launch.yaml
  - mobile/maestro/flows/navigation.yaml
  - mobile/maestro/flows/theme-switch.yaml
  - mobile/maestro/config.yaml
autonomous: false
requirements:
  - FOUND-08

must_haves:
  truths:
    - "Maestro CLI can launch the app on iOS Simulator and run a basic flow test"
    - "Maestro captures screenshots of each tab screen"
    - "Navigation flow test verifies all 5 tabs are reachable"
    - "Theme switch flow test can change themes and capture before/after screenshots"
  artifacts:
    - path: "mobile/maestro/config.yaml"
      provides: "Maestro configuration with app ID"
      contains: "appId"
    - path: "mobile/maestro/flows/app-launch.yaml"
      provides: "Basic launch test with screenshot capture"
      contains: "takeScreenshot"
    - path: "mobile/maestro/flows/navigation.yaml"
      provides: "Tab navigation verification flow"
      contains: "assertVisible"
    - path: "mobile/maestro/flows/theme-switch.yaml"
      provides: "Theme switching verification flow"
      contains: "takeScreenshot"
  key_links:
    - from: "mobile/maestro/flows/app-launch.yaml"
      to: "mobile app binary"
      via: "Maestro launches app via appId"
      pattern: "launchApp"
    - from: "mobile/maestro/flows/navigation.yaml"
      to: "mobile/app/(tabs)/_layout.tsx"
      via: "Maestro taps on tab labels to navigate"
      pattern: "tapOn"
---

<objective>
Set up Maestro E2E test infrastructure with YAML flow tests that verify app launch, tab navigation, and theme switching on iOS Simulator, with screenshot capture for visual validation.

Purpose: Establishes the testing foundation used throughout all future phases. Maestro tests will validate every screen as it is built. This plan also serves as the first real verification that the app renders correctly on a device.

Output: Maestro config, 3 flow test files, and captured screenshots proving the app works.
</objective>

<execution_context>
@C:/Users/Arthur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Arthur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 and 02 outputs -->

App configuration:
- App ID: com.myrlin.mobile (from app.json slug)
- Tab labels visible in UI: "Sessions", "Tasks", "Costs", "Docs", "More"
- Default theme: Catppuccin Mocha (dark)

Maestro requirements (from research):
- Maestro iOS requires macOS with Xcode + idb-companion
- Run on Mac Mini (arthurs-mac-mini / 100.111.181.106) via Tailscale
- Build iOS simulator binary via: eas build --profile development-simulator --platform ios
- Install on simulator via: xcrun simctl install booted <app.app>
- Maestro CLI: brew install maestro (on Mac)

Maestro YAML syntax (from design doc):
```yaml
appId: com.myrlin.mobile
---
- launchApp:
    clearState: true
- assertVisible: "Sessions"
- takeScreenshot: screenshot-name
- tapOn: "Tasks"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Maestro config and 3 flow test files</name>
  <files>
    mobile/maestro/config.yaml,
    mobile/maestro/flows/app-launch.yaml,
    mobile/maestro/flows/navigation.yaml,
    mobile/maestro/flows/theme-switch.yaml
  </files>
  <action>
    1. Create `mobile/maestro/config.yaml`:
       ```yaml
       appId: com.myrlin.mobile
       ```

    2. Create `mobile/maestro/flows/app-launch.yaml`:
       - Launch app with clearState: true
       - Assert "Sessions" tab is visible (default tab)
       - Take screenshot: "app-launch-sessions"
       - This is the minimal smoke test to prove the app runs

    3. Create `mobile/maestro/flows/navigation.yaml`:
       - Launch app with clearState: true
       - For each of the 5 tabs (Sessions, Tasks, Costs, Docs, More):
         - tapOn the tab label
         - assertVisible the tab name text on screen
         - takeScreenshot with descriptive name (e.g., "nav-sessions", "nav-tasks", etc.)
       - This verifies FOUND-03: all tab screens are reachable via navigation

    4. Create `mobile/maestro/flows/theme-switch.yaml`:
       - Launch app with clearState: true
       - Navigate to More tab
       - Take screenshot: "theme-mocha-default" (baseline Mocha theme)
       - This flow will be expanded in later phases when the theme picker UI is built
       - For now, it captures the default theme state for comparison

    5. Create `mobile/maestro/screenshots/` directory (empty, for captured screenshots)

    NOTE: Maestro tests can only be RUN on macOS with iOS Simulator. The YAML files are created here but actual execution requires the Mac Mini. The checkpoint below handles the execution verification.
  </action>
  <verify>
    <automated>ls mobile/maestro/flows/*.yaml | wc -l</automated>
  </verify>
  <done>
    - maestro/config.yaml exists with appId
    - 3 flow YAML files exist: app-launch.yaml, navigation.yaml, theme-switch.yaml
    - Each flow uses launchApp, assertVisible, and takeScreenshot commands
    - navigation.yaml tests all 5 tabs
    - screenshots/ directory exists for capture output
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 1 Foundation</name>
  <action>Human verification of all Phase 1 deliverables across Plans 01, 02, and 03.</action>
  <what-built>
    Complete Phase 1 Foundation: Expo project scaffold, EAS Build config, 13 Catppuccin themes extracted from web CSS, MMKV-backed theme store, 5-tab navigation with themed UI, 17 shared UI components, Maestro test flow files.
  </what-built>
  <how-to-verify>
    1. Build check: Run `cd mobile && npx expo export --platform web` to verify compilation
    2. Theme parity spot check: Open `mobile/theme/tokens.ts` and compare 3-5 color values for Mocha theme against `src/web/public/styles.css` :root CSS custom properties. They should match exactly.
    3. Component count: Run `ls mobile/components/ui/*.tsx | wc -l` and confirm 17 files
    4. No hardcoded colors: Run `grep -r '#[0-9a-fA-F]\{6\}' mobile/components/ui/ | grep -v 'import\|require\|//'` to check for hardcoded hex values in components (should return nothing)
    5. EAS config: Open `mobile/eas.json` and verify 4 build profiles exist
    6. Maestro flows: Open `mobile/maestro/flows/navigation.yaml` and verify it tests all 5 tabs
    7. (Optional, if Mac available): Build for iOS Simulator with `eas build --profile development-simulator --platform ios`, install on simulator, and run `maestro test mobile/maestro/flows/app-launch.yaml`
  </how-to-verify>
  <verify>
    <automated>cd mobile && npx expo export --platform web 2>&1 | tail -3</automated>
  </verify>
  <done>Human confirms Phase 1 foundation is correct and complete</done>
  <resume-signal>Type "approved" if the foundation looks correct, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- 3 Maestro YAML flow files exist and are valid YAML
- app-launch.yaml tests basic app launch with screenshot
- navigation.yaml tests all 5 tab transitions
- theme-switch.yaml captures default theme baseline
- (When run on Mac): Maestro flows pass on iOS Simulator
</verification>

<success_criteria>
- Maestro test infrastructure is set up with config and 3 flow files
- Flow files cover app launch, all tab navigation, and theme baseline capture
- Human verification confirms the full Phase 1 foundation is correct
- Ready for Phase 2 (Connection and Auth) which builds on this foundation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
