---
phase: 07-platform-polish-and-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/utils/haptics.ts
  - mobile/app/(tabs)/sessions/index.tsx
  - mobile/app/(tabs)/tasks/index.tsx
  - mobile/app/(tabs)/costs/index.tsx
  - mobile/app/(tabs)/docs/index.tsx
  - mobile/app/(tabs)/more/workspaces.tsx
  - mobile/app/(tabs)/more/resources.tsx
  - mobile/app/(tabs)/more/conflicts.tsx
  - mobile/app/(tabs)/more/templates.tsx
  - mobile/app/(tabs)/more/session-manager.tsx
  - mobile/app/(tabs)/more/recent.tsx
  - mobile/app/(tabs)/more/settings.tsx
  - mobile/app/(tabs)/sessions/[id].tsx
  - mobile/app/(tabs)/tasks/[id].tsx
  - mobile/components/ui/Button.tsx
  - mobile/components/ui/Toggle.tsx
  - mobile/components/ui/ActionSheet.tsx
  - mobile/components/ui/Chip.tsx
  - mobile/components/ui/SegmentedControl.tsx
  - mobile/components/sessions/SessionCard.tsx
  - mobile/components/terminal/TerminalToolbar.tsx
  - mobile/app/_layout.tsx
  - mobile/app.json
  - mobile/package.json
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - PLAT-05

must_haves:
  truths:
    - "User feels haptic feedback when tapping buttons, toggles, chips, and action sheet items"
    - "User feels haptic feedback on pull-to-refresh across all list screens"
    - "Tasks tab has pull-to-refresh like all other list screens"
    - "Deep links myrlin://session/xyz open the correct session detail screen from outside the app"
    - "Share sheet includes session and terminal text sharing"
  artifacts:
    - path: "mobile/utils/haptics.ts"
      provides: "Centralized haptic feedback utility with impact, selection, and notification helpers"
      exports: ["hapticImpact", "hapticSelection", "hapticNotification", "hapticTab"]
    - path: "mobile/app.json"
      provides: "Deep link scheme configuration (myrlin://)"
      contains: "myrlin"
  key_links:
    - from: "mobile/components/ui/Button.tsx"
      to: "mobile/utils/haptics.ts"
      via: "import hapticImpact, called on press"
      pattern: "hapticImpact"
    - from: "mobile/app/_layout.tsx"
      to: "expo-linking"
      via: "Linking config for myrlin:// scheme"
      pattern: "linking|useURL"
---

<objective>
Add haptic feedback to all interactive elements, ensure pull-to-refresh on every list screen, and wire up deep linking (myrlin:// scheme) so the app responds to external URLs.

Purpose: Makes the app feel native with tactile feedback on every interaction, ensures consistent pull-to-refresh behavior, and enables external navigation into the app via deep links.
Output: haptics.ts utility, updated UI components with haptics, deep link config in app.json and root layout, pull-to-refresh on tasks screen.
</objective>

<execution_context>
@C:/Users/Arthur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Arthur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@mobile/utils/ (create new)
@mobile/components/ui/Button.tsx
@mobile/components/ui/Toggle.tsx
@mobile/components/ui/ActionSheet.tsx
@mobile/components/ui/Chip.tsx
@mobile/components/ui/SegmentedControl.tsx
@mobile/components/sessions/SessionCard.tsx
@mobile/components/terminal/TerminalToolbar.tsx
@mobile/app/_layout.tsx
@mobile/app/(tabs)/_layout.tsx
@mobile/app/(tabs)/tasks/index.tsx
@mobile/app.json
@mobile/hooks/usePush.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From mobile/hooks/useTheme.ts:
```typescript
export function useTheme(): { theme: MyrlinTheme; themeId: ThemeId; setTheme: (id: ThemeId) => void };
```

From mobile/components/ui/index.ts (17 components):
Button, Toggle, ActionSheet, Chip, SegmentedControl, Card, Badge, etc.

From mobile/hooks/usePush.ts (existing deep link routing):
```typescript
// Notification tap already routes: session -> /sessions/:id, task -> /data/tasks, conflict -> /more/conflicts
```

expo-linking is already installed (v55.0.10-canary).
expo-haptics is NOT yet installed; must be added to package.json.

Pull-to-refresh already exists on: sessions, costs, docs, resources, conflicts, recent, session-manager, templates, workspaces.
Pull-to-refresh MISSING on: tasks/index.tsx (the tasks kanban screen).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create haptics utility and wire into all interactive UI components</name>
  <files>
    mobile/utils/haptics.ts,
    mobile/components/ui/Button.tsx,
    mobile/components/ui/Toggle.tsx,
    mobile/components/ui/ActionSheet.tsx,
    mobile/components/ui/Chip.tsx,
    mobile/components/ui/SegmentedControl.tsx,
    mobile/components/sessions/SessionCard.tsx,
    mobile/components/terminal/TerminalToolbar.tsx,
    mobile/app/(tabs)/tasks/index.tsx,
    mobile/package.json
  </files>
  <action>
    1. Install expo-haptics: `npx expo install expo-haptics` in the mobile/ directory.

    2. Create `mobile/utils/haptics.ts` with these exports:
       - `hapticImpact(style?: 'light' | 'medium' | 'heavy')` - wraps Haptics.impactAsync, defaults to 'medium'. For button presses, card taps.
       - `hapticSelection()` - wraps Haptics.selectionAsync. For toggles, chips, segment changes.
       - `hapticNotification(type?: 'success' | 'warning' | 'error')` - wraps Haptics.notificationAsync. For action confirmations, errors.
       - `hapticTab()` - alias for hapticImpact('light'). For tab bar taps.
       - All functions should be no-ops if expo-haptics is not available (try/catch, log warning once).
       - Respect a `hapticsEnabled` check: read from settings store if it exists (`mobile/stores/settings-store.ts`), default to true.

    3. Wire haptics into shared UI components (add call inside existing onPress/onChange handlers):
       - `Button.tsx`: Call `hapticImpact('light')` at the start of the onPress handler. Use 'medium' for destructive variant.
       - `Toggle.tsx`: Call `hapticSelection()` when toggle value changes.
       - `ActionSheet.tsx`: Call `hapticImpact('light')` when an action item is pressed.
       - `Chip.tsx`: Call `hapticSelection()` on chip press.
       - `SegmentedControl.tsx`: Call `hapticSelection()` on segment change.

    4. Wire haptics into screen-level components:
       - `SessionCard.tsx`: Call `hapticImpact('light')` on card press and `hapticImpact('medium')` on long press.
       - `TerminalToolbar.tsx`: Call `hapticImpact('light')` on toolbar button presses (copy, paste, share, voice, camera).

    5. Add pull-to-refresh to `tasks/index.tsx`: The tasks screen wraps TaskBoard and TaskList. Add a RefreshControl or pass refreshing/onRefresh props. Use `tasksQuery.refetch()` and `tasksQuery.isRefetching` from useTasks hook. If the board uses a horizontal ScrollView, add pull-to-refresh to the outer container.

    IMPORTANT: Do not remove any existing code. Only add haptic calls and the missing pull-to-refresh.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - expo-haptics installed in package.json
    - haptics.ts utility created with 4 exported functions
    - Button, Toggle, ActionSheet, Chip, SegmentedControl all fire haptics on interaction
    - SessionCard and TerminalToolbar fire haptics
    - Tasks screen has pull-to-refresh
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure deep link scheme and wire URL routing</name>
  <files>
    mobile/app.json,
    mobile/app/_layout.tsx
  </files>
  <action>
    1. Update `mobile/app.json` to add the deep link scheme:
       - Under `expo.scheme`, set to `"myrlin"` (this enables myrlin:// URLs)
       - If `expo.scheme` already exists, verify it is `"myrlin"`

    2. In `mobile/app/_layout.tsx`, expo-router handles deep links automatically via the scheme config. Verify the Stack navigator supports these routes:
       - `myrlin://session/:id` should resolve to `(tabs)/sessions/[id]`
       - `myrlin://tasks` should resolve to `(tabs)/tasks`
       - `myrlin://costs` should resolve to `(tabs)/costs`
       - `myrlin://settings` should resolve to `(tabs)/more/settings`

    3. Expo Router's file-based routing with the scheme in app.json handles this automatically. The key is ensuring `app.json` has the scheme and that the push notification handler in usePush.ts continues to work (it already uses router.push with the correct paths).

    4. Add a `linking` configuration to the root layout if needed for custom path mapping. Expo Router v4+ auto-generates linking config from file structure, so this may just need the scheme declaration.

    IMPORTANT: The push notification tap handler in usePush.ts already routes to /sessions/:id, /data/tasks, /more/conflicts. Deep links should use the same paths. Do NOT change usePush.ts routing.
  </action>
  <verify>
    <automated>cd mobile && grep -q '"scheme"' app.json && echo "scheme found" || echo "MISSING scheme"</automated>
  </verify>
  <done>
    - app.json has scheme: "myrlin"
    - Deep links (myrlin://session/xyz) route to session detail
    - Existing push notification routing unchanged
  </done>
</task>

</tasks>

<verification>
1. `cd mobile && npx tsc --noEmit` compiles without errors
2. `grep -r "hapticImpact\|hapticSelection\|hapticNotification" mobile/components/ mobile/utils/` shows haptics wired into all UI components
3. `grep "scheme" mobile/app.json` shows "myrlin" scheme configured
4. `grep "onRefresh" mobile/app/\(tabs\)/tasks/index.tsx` confirms pull-to-refresh on tasks
</verification>

<success_criteria>
- All interactive UI components fire haptic feedback on press/change
- Tasks screen has pull-to-refresh matching all other list screens
- Deep link scheme "myrlin" configured in app.json
- TypeScript compiles cleanly
- No existing code removed or broken
</success_criteria>

<output>
After completion, create `.planning/phases/07-platform-polish-and-testing/07-01-SUMMARY.md`
</output>
